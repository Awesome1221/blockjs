/*
  Functions may be blank as they have no JavaScript side code an are just for Java to hook onto
  and are for Intellisense purposes only.
*/

export function sendMessage(message: string) {
  // Send message to server, see 'Chat.java' for implementation
}